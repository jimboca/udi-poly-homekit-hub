# PG3x Node Server runtime (install on the Polyglot host, not dev sandboxes).
# Requires Python 3.10+ (aiohomekit 3.x and current udi_interface wheels).
udi_interface>=3.0.31
aiohomekit>=3.2.0,<4
websockets>=12.0
zeroconf>=0.132.2
markdown2
